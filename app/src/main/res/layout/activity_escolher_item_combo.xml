<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_escolher_item_combo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".cardapioOnline.activitys.EscolherItemComboActivity">

    <include
        android:id="@+id/toolbar_escolher_item_combo"
        layout="@layout/app_bar_layout" />

    <ProgressBar
        android:id="@+id/progressbar_escolher_item_combo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:theme="@style/MyProgressbarColor"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/layout_list_item_combo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?actionBarSize"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/desc_item_combo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="Sabores disponíveis para este combo"
            android:textSize="17sp" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/list_item_combo"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        </android.support.v7.widget.RecyclerView>
    </LinearLayout>
</RelativeLayout>